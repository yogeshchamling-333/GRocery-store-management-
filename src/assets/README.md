Place any local images (logo, icons, etc.) here and import them directly
in your components, e.g. `import logo from '../assets/logo.png'`.

This project currently uses hosted Unsplash placeholder images for
product photos (see src/data/products.js), so nothing is required here
to run the app — this folder exists to match the requested project
structure.
